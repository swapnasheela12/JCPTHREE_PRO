import { TableAgGridDirective } from './table-ag-grid.directive';

describe('TableAgGridDirective', () => {
  it('should create an instance', () => {
    const directive = new TableAgGridDirective();
    expect(directive).toBeTruthy();
  });
});
