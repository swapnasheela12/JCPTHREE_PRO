import { CommentHighlightFieldDirective } from './comment-highlight-field.directive';

describe('CommentHighlightFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentHighlightFieldDirective();
    expect(directive).toBeTruthy();
  });
});
