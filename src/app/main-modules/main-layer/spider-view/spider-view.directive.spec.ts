import { SpiderViewDirective } from './spider-view.directive';

describe('SpiderViewDirective', () => {
  it('should create an instance', () => {
    const directive = new SpiderViewDirective();
    expect(directive).toBeTruthy();
  });
});
