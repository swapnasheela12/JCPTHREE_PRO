import { MmenuDirective } from './mmenu.directive';

describe('MmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MmenuDirective();
    expect(directive).toBeTruthy();
  });
});
