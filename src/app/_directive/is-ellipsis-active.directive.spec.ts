import { IsEllipsisDirectiveDirective } from './is-ellipsis-directive.directive';

describe('IsEllipsisDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEllipsisDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
